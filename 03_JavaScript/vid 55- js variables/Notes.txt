1. var a = 5.6;
   var b = 6;
   var c = "mohan"; 
    -like this any variable can be declared
    -js automatically recognise the data type

2. var - this is global scope variable 
   let - this is block scope variable
   const - this is also block scope variable

3. Declaring object

   let o = {
      name: "Akash",
      // this is an key value pair here "name" is key and "Akash" is value

      "job code": 5600,
      // here name is not written in " " and job code is written in " " bcz name does not contain any spaces and job name contains the spaces 

      is_handsome: true
   }

4. Adding elements in object
   o.salary = "100crores"

